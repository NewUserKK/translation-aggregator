export class UserStoreState {
  constructor() {
  }
}

export enum UserStoreMutation {
}

export enum UserStoreAction {

}

const UserStoreStore = {
  state: new UserStoreState(),

  mutations: {},

  actions: {}
};

export default UserStoreStore;
